# CR-13 evidence

Join-code contract and workspace schema are implemented: case-sensitive eight-character code, uniqueness, rotation epoch, pending join requests and owner-only rotation. Join code remains routing metadata and does not grant membership or keys.

Validation: join-code unit tests, local migration reset, pgTAP suite and schema lint pass.
